package constants

const (
	PortDefaultValue = 8080
)

const (
	ServerRunningMessage = "Server is running on port %d"
)

// DNS String Configuration
const (
	DNSString = "host=%s port=%s user=%s password=%s dbname=%s sslmode=%s TimeZone=%s"
)

const (
	PostgresConnectionSuccessful = "Postgres connection successful"
)

// gst billing service constant
const (
	UserName            = "username"
	ProductAddedMessage = "Product added successfully"
)
